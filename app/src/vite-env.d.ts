/// <reference types="vite/client" />

/** Hash corto del commit con el que se compiló esta build. */
declare const __VERSION__: string
/** Momento de la compilación, en ISO. */
declare const __COMPILADO__: string
