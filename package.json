{
  "name": "grifo",
  "private": true,
  "description": "Atajos desde la raíz del repo. La app vive en app/ — esto solo evita tener que acordarse del cd.",
  "scripts": {
    "dev": "npm --prefix app run dev",
    "build": "npm --prefix app run build",
    "preview": "npm --prefix app run preview",
    "typecheck": "npm --prefix app run typecheck",
    "postinstall": "npm --prefix app install"
  }
}
